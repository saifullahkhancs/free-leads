# Base URL of the Node/Express API. Leave empty in dev if you configure a
# Vite proxy for /api instead (see vite.config.js — proxies to
# http://localhost:8004). An empty value means the frontend calls the
# same origin via the Vite dev-server proxy (recommended).
VITE_API_URL=
